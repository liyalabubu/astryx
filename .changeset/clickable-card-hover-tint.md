---
'@astryxdesign/core': patch
---

[fix] ClickableCard now tints its background on hover/active instead of overlaying a pseudo-element, so tinted variants (blue, muted, transparent, etc.) no longer show a faint 1px untinted ring around the card edge.
@kentonquatman
